#include <stdio.h>
int main(){
  char letra;
  // int a;
  // a = 10;
  // letra = 'L';
  // letra = letra + a%2;
  // printf("a = %d e letra = %c. \n", a, letra);
  // return 0;

  // int a = 3, b = 2, c;
  // c=a!=b;

  // printf("%d, %d, %d", a, b, c);

  // char letter;

  // printf("Enter a letter: ");
  // scanf("%c", &letter);
  // letter = letter + ('A' - 'a');
  // printf("The letter is: %c\n", letter);
  // return 0;

  // int n, i;
  // i = 20;
  // n = 2 * (i%3);
  // printf("resto = %d\n", i%3);
  // printf("n = %d\n", n);

  // int a, *b, c;
  // a = 5;
  // b = &a;
  // c = 10;
  // scanf("%d", b);
  // printf("%d %d\n", a, c);

  int *b;
  scanf("%d", b);
  printf("%d\n", *b);
}